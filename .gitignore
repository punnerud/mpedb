/target
Cargo.lock
*.mpedb
__pycache__/
.venv/
.claude/
# Crates the parent workspace `exclude`s get their own target dir rather than
# sharing the root one. `/target` only anchors at the repo root, so each needs
# its own line — a missed one gets a build tree staged by `git add -A`.
crates/mpedb-capi/target/
crates/mpedb-olapbench/target/
