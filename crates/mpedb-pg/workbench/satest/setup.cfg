[sqla_testing]
requirement_cls=requirements:Requirements
profile_file=profiles.txt

# Point this at a running `forkserve.py`. The socket DIRECTORY is the host.
[db]
default=postgresql+psycopg2://morten@/app?host=/mnt/ext4/pgwb/sock
